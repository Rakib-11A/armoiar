$('#compare-modal').modal().find('#compare_content').html("<%= escape_javascript(render 'products/compare_products', products: @products).html_safe %>");
if ($.fn.rateit) { $('.star-rating').rateit(); }